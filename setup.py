from setuptools import setup, find_packages
# -e. in requirements.txt will look for this package and it will install all the folders as local package.
setup(
    name="us_visa",
    version="0.1",
    author="pranshu",
    author_email="",
    packages=find_packages()
)